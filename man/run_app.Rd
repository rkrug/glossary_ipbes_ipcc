% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{run_app}
\alias{run_app}
\title{Run the IPBES/IPCC Glossary Comparison Shiny App}
\usage{
run_app(
  cache_dir = tools::R_user_dir("glossary.ipbes.ipcc", which = "cache"),
  enable_live_update = .default_live_update_enabled(),
  ...
)
}
\arguments{
\item{cache_dir}{Directory used to store:
\itemize{
\item The user-updated IPCC glossary CSV (written by the "Update" button).
}

Defaults to a package-specific subdirectory of the OS user cache directory
as returned by \code{\link[tools:userdir]{tools::R_user_dir()}}.  The directory is created
automatically if it does not exist.}

\item{enable_live_update}{Logical; whether the "Update IPCC Glossary" control
is enabled. Defaults to \code{.default_live_update_enabled()}, which disables
live updates on hosted shinyapps.io deployments and enables them locally.}

\item{...}{Additional arguments passed to \code{\link[shiny:shinyApp]{shiny::shinyApp()}} (e.g.
\code{launch.browser}, \code{port}).}
}
\value{
Invisibly, the \code{\link[shiny:shinyApp]{shiny::shinyApp()}} object (only relevant when
\code{launch.browser = FALSE}).
}
\description{
Launches the interactive Shiny application that displays IPBES and IPCC
glossary definitions side-by-side with similarity scores and word-level
diffs.
}
\examples{
\dontrun{
glossary.ipbes.ipcc::run_app()
}
}
